# Copy to .streamlit/secrets.toml for local runs, OR paste into the
# Streamlit Cloud "Secrets" box (App → Settings → Secrets).
# The API key is billed separately from any Claude.ai subscription.
ANTHROPIC_API_KEY = "sk-ant-..."
